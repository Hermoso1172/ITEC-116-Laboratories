function CreateTaskModal({ isOpen, setIsOpen }) {
  return (
    <div className="fixed top-0 left-0 w-full h-full bg-black/30 flex justify-center items-center">
      CreateTaskModal
    </div>
  );
}

export default CreateTaskModal;
